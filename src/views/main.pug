doctype html
html 
    head 
        title MAIN
        meta(charset="utf-8")
    body
        h1
        if !id
            a(href='/auth/github') Login With Github
        else 
            p #{id}
            |  
            a(href=`${address}`) My Github Home
            |  
            a(href='/auth/logout') Logout